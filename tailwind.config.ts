const config = {
	theme: {
		extend: {
			fontFamily: {
				sans: ["var(--font-schibsted-grotesk)", "sans-serif"],
				mono: ["var(--font-martian-mono)", "monospace"],
			},
		},
	},
};

export default config;
